# Requires: nvme-cli installed.
# Safe: `nvme list` is read-only and exits 0 even when no NVMe devices are
# present, so this is safe to run on any system with nvme-cli installed.
TEST "NVMe List"

RUN "nvme list"

EXPECT_EXIT 0

END
